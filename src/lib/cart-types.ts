export type CartItem = {
  variantId: string;
  productSlug: string;
  productName: string;
  variantLabel: string;
  priceCents: number;
  image: string;
  isDigital: boolean;
  quantity: number;
};
